const BaseColor = {
  black: '#000',
  white: '#fff',
  heliotrope: '#e43df0',
  amaranth: 'rgba(240, 61, 80, 0.15)',
};

const ThemeColors = {
  default: {
    primaryText: BaseColor.black,
    primaryBackground: BaseColor.white,
  },
  valentine: {
    primaryText: BaseColor.heliotrope,
    primaryBackground: BaseColor.amaranth,
  },
};

export default ThemeColors;
